-- Backward-compatible Route Campaign adapter.
-- All encounter, movement, battle, flee, and capture behavior lives in the
-- shared wild_engine module used by both Wild Encounters and Campaign.

return require("wild_engine")
